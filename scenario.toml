# AgentBeats Scenario Configuration
# Fill in agent IDs after registration on agentbeats.dev

[green]
# Green agent (benchmark evaluator) — fill per benchmark
agentbeats_id = ""  # varies: e.g., "kmadorin/ethernaut-arena"

[purple]
agentbeats_id = "Startlight985/startlight-cyber"
image = "ghcr.io/startlight985/cyber-agent:main"

[purple.env]
ANTHROPIC_API_KEY = "${ANTHROPIC_API_KEY}"

[assessment]
timeout = 600
